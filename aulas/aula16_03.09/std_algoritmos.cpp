vector e array

min
max
swap

sort(begin, end, compara)
random_shuffle(begin, end)
find(begin, end, value) retorna pointer
search(begin, end, begin, end)

copy(comeco, fim, destino)
reverse_copy(comeco, fim, destino)
reverse(begin, end)

count(begin, end, valor)
for_each(begin, end, funcao)


remove_if(begin, end, funcao)
replace_if(begin, end, valor, novo_valor)

min_element retorna pointer
max_element retorna pointer
